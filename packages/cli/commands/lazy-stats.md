---
description: Show the honest token ledger — injections, skips, and on-device calibration state
---
Run this exact command with the Bash tool:

```
node "${CLAUDE_PLUGIN_ROOT}/hooks/lazy-stats.mjs"
```

Show its output to the user verbatim in a code block. Add nothing.
